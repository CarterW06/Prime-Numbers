import java.util.ArrayList;

public class PrimeList {
	// inclusive
	@SuppressWarnings("rawtypes")
	public ArrayList primeList(int start, int end) {
		ArrayList<Integer> alint = new ArrayList<>();
		for(int i = start; i <= end; i++) {
			
		}
		return alint;
	}
}
